apiVersion: v1
kind: Service
metadata:
  name: {{ include "bbs.fullname" . }}
  labels:
    {{- include "bbs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bbs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
