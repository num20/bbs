{{ .Values.bbs.title }} has been deployed.

{{- if .Values.ingress.enabled }}

  {{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/
  {{- end }}
{{- else }}

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "bbs.fullname" . }} 3000:{{ .Values.service.port }}
  → http://localhost:3000/
{{- end }}

{{- if not .Values.litestream.enabled }}

WARNING: Litestream is disabled. If the PVC is lost, your data will be lost too.
{{- end }}
