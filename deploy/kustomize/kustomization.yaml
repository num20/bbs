# kubectl apply -k deploy/kustomize で適用します
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bbs

resources:
  - namespace.yaml
  - statefulset.yaml
  - service.yaml

# アプリのイメージ（GitHub Actions の publish-image で ghcr.io に公開）
images:
  - name: bbs
    newName: ghcr.io/num20/bbs
    newTag: latest

configMapGenerator:
  # アプリと Litestream の設定値
  - name: bbs-config
    envs:
      - config.env
  # Litestream の設定ファイル
  - name: litestream
    files:
      - litestream.yml

secretGenerator:
  # ソルトと S3 の認証情報（secret.env.example をコピーして作成）
  - name: bbs-secret
    envs:
      - secret.env
